include_rules

EXE := TSP_File
CXX_LIBS += -lfmt
CXX_FLAGS += -IAlgorithms -I.

: foreach *.cpp | *.hpp |> !cxx |> {objs}
: {objs} | Algorithms/*.o |> $(CXX) -o %o %f $(CXX_LIBS) $(CXX_FLAGS) |> $(EXE)
